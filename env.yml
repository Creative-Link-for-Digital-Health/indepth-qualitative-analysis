name: indepth-qual
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults

dependencies:
  - python=3.11
  - pip
  - streamlit
  # PyTorch + CUDA meta package instead of specifying cudatoolkit manually
  - pytorch
  - torchvision
  - torchaudio
  - pytorch-cuda=12.1

  - pip:
    - python-docx
    - openai
    - toml
    - docx2txt
    - tiktoken
    - PyPDF2
    - clipboard
    - pydantic>=2.0
    - whisperx

# ──────────────── Commands ────────────────
# Create the environment from this file:
#   conda env create -f env.yml
#
# Activate the environment:
#   conda activate indepth-qual
#
# If you make changes to this file and want to sync them:
#   conda env update -f env.yml --prune
#
# List all environments:
#   conda env list
#
# Deactivate the current environment:
#   conda deactivate
#
# Remove this environment entirely:
#   conda env remove -n indepth-qual